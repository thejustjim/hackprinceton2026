# Backend API base URL used by Next.js server routes under frontend/app/api.
# Defaults to http://127.0.0.1:8000 when unset, so this is optional for local dev.
# Do not append /api.
API_BASE_URL=http://127.0.0.1:8000
